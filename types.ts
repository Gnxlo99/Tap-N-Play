
export interface Game {
  id: string;
  title: string;
  genre: string;
  imageUrl: string;
  heroImageUrl: string;
  rating: number;
  description: string;
  playUrl: string;
  instructions: string;
}